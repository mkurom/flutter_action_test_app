import 'package:flutter/material.dart';

class TickerTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TickerFuture'),
      ),
      body: Container(
        child: Center(
          child: Text('sample'),
        ),
      ),
    );
  }
}
